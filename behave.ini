[behave]
# Feature files live under ./features; step definitions are loaded via features/environment.py
paths = features
junit = true
junit_directory = reports

# Default run: pretty output. CI example: behave -f junit -o reports/junit.xml
color = true
stdout_capture = true
stderr_capture = true
logging_level = INFO
show_timings = true
